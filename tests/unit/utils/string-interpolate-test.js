import stringInterpolate from '../../../utils/string-interpolate';
import { module, test } from 'qunit';

module('stringInterpolate');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = stringInterpolate();
  assert.ok(result);
});
