{
  "name": "eurekapp",
  "version": "0.0.0",
  "description": "",
  "devDependencies": {
    "gulp-concat": "^2.2.0",
    "gulp": "^3.5.6",
    "gulp-ember-handlebars": "^0.6.0"
  },
  "scripts": {
    "postinstall": "bower install && gulp",
    "build": "gulp",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Namlook",
  "license": "MIT"
}
