
<div class="eureka-title-section">
    <div class="col-xs-7 col-sm-7">
        <h1> {{__modelMeta__.pluralizedLabel}} </h1>

{{!--         <ul class="nav nav-pills">
            <li class="dropdown active">
                <a class="dropdown-toggle"  data-toggle="dropdown" href="#">
                    <span class="badge pull-left">topic</span>
                    &nbsp;iPhone 5S
                    <span class="caret"></span>
                </a>
                <ul class="dropdown-menu">
                    <li><a href="#">filter</a></li>
                    <li><a href="#">remove</a></li>
                </ul>
            </li>
            <li class="dropdown active">
                <a href="#">
                    <span class="badge pull-left">topic</span>
                    &nbsp;Online service provider &times;
                </a>
            </li>
            <li class="dropdown active">
                <a href="#">
                    <span class="badge pull-left">author</span>
                    &nbsp;Namlook &times;
                </a>
            </li>
            <li class="dropdown active">
                <a href="#">
                    <span class="badge pull-left">topics.nuggets.author.age</span>
                    &nbsp; > 18 &times;
                </a>
            </li>
        </ul> --}}
    </div>
    <div class="col-xs-5 col-sm-5 text-right eureka-link-to-new-section">
        {{#link-to __modelMeta__.newRouteName
            classNames="btn btn-default eureka-link-to-new"
            classNameBindings="__modelMeta__.EurekaGenericModelModelCSS"}}
                + Create {{__modelMeta__.label}}
        {{/link-to}}
    </div>
</div>

<div {{bind-attr class=":eureka-filter-section filters.length::eureka-filter-section-empty"}}>
    <ul class="nav nav-tabs">
        {{#if filters.length}}
            {{#link-to __modelMeta__.indexRouteName
                tagName="li"
                href=false
                classNames="eureka-link-to-index"
                classNameBindings="__modelMeta__.EurekaGenericModelModelCSS"}}
                    <a href="#">All</a>
            {{/link-to}}
            {{#each filter in filters}}
                {{#link-to filter.route
                    tagName="li"
                    href=false
                    classNames="eureka-filter-to"
                    classNameBindings="__modelMeta__.EurekaGenericModelModelCSS"}}
                        <a href="#">
                            <i {{bind-attr class="filter.icon"}}></i>
                            <span class="hidden-xs">{{filter.label}}</span>
                        </a>
                {{/link-to}}
            {{/each}}
        {{/if}}
    </ul>
</div>

<div class="row">
    <!-- results -->
    {{#if facetedFields}}
    <div class="col-xs-9 col-sm-9">
    {{else}}
    <div class="col-xs-12 col-sm-12">
    {{/if}}
        <form class="eureka-search-section form-inline text-right" role="form">
            <div class="form-group has-feedback eureka-search-query-container">
                <span class="glyphicon glyphicon-search form-control-feedback search-icon"></span>
                {{#search-query action="searchModel" modelMeta=__modelMeta__}}{{/search-query}}
            </div>
        </form>
        <div class="eureka-results clearfix">
            {{#if tableView}}
                {{!-- {{partial 'components/generic_model-list-table'}} --}}
                {{model-list-table modelMeta=__modelMeta__
                    query=query
                    itemClicked='displayItem'}}
            {{else}}
                {{!-- {{partial 'components/generic_model-list'}} --}}
                {{model-list modelMeta=__modelMeta__ query=query}}
            {{/if}}
        </div>
    </div>

    <!-- search and faceting -->
    {{#if facetedFields}}
    <div class="col-xs-3 col-sm-3 eureka-right-sidebar">
        <h3>Stats</h3>
        {{#each facetedField in facetedFields}}
            {{faceted-field fieldPath=facetedField.fieldPath
                fieldName=facetedField.fieldName
                query=query
                limit=facetedField.limit
                modelType=model.type
                action='searchModel'}}
        {{/each}}

    </div>
    {{/if}}
</div>
