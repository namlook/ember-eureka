
{{#if isArray}}
    <ul>
        {{#each val in value}}
            <li>
            {{#if val._id}}
                {{#link-to 'type.display' val._type val._id}}{{val.title}}{{/link-to}}
            {{else}}
                {{val}}
            {{/if}}
            </li>
        {{/each}}
    </ul>
{{else}}
    {{#if isRelation}}
        {{log value.title}}
        {{#link-to 'type.display' value._type value._id}}{{value.title}}{{/link-to}}
    {{else}}
        {{value}}
    {{/if}}
{{/if}}
