<h5>{{fieldName}}</h5>
{{#if isLoading}}
    <i>loading stats on {{fieldName}}...</i>
{{else}}
    <ul>
        {{#each item in results}}
            <li>{{item.facet}} ({{item.count}})</li>
        {{/each}}
    </ul>
{{/if}}