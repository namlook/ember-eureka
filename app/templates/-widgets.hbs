
{{#if currentRouteController.meta.isOutlet}}
    {{#widget-container
        config=currentRouteController.meta
        routeModel=model
        currentController=currentRouteController
        toControllerAction="toControllerAction"}}
            {{outlet}}
    {{/widget-container}}
{{else}}
    {{widget-container
        config=currentRouteController.meta
        routeModel=model
        currentController=currentRouteController
        toControllerAction="toControllerAction"}}
{{/if}}